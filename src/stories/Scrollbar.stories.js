import React from 'react';
import { Scrollbar } from '../components/Scrollbar';



export default {
  title: 'Sidebar',
  component: Scrollbar,
}

export const Scroll = ({ ...props }) => (
              
        <Scrollbar {...props}>   
        </Scrollbar>

)
  